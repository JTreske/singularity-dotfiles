hl.config({
  general = {
    -- ! Only define layout here (other values are defined in conf.general)
    layout = "dwindle"
  },
  dwindle = {
    preserve_split = true,
    smart_split = true,
  }
})
